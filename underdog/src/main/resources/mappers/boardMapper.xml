<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.underdog.mapper.boardMapper">

	<insert id="create">
		insert into board (bo_me_email,
							 bo_bbsid,
							 bo_me_nick,
							 bo_title,
							 bo_concent,
							 bo_bbsid,
							 bo_regip,
							 bo_regdate,
							 bo_regip)
		values(#{bo_me_email},
				#{bo_bbsid},
		 		#{bo_me_nick},
		 		#{bo_title},
		 		#{bo_concent},
		 		#{bo_bbsid},
		 		#{bo_regip},
		 		sysdate,
		 		#{bo_regip})
		 		<!-- 아이피 삭제유무 삭제일 이해안됨 -->
	</insert>
	
<select id="list">

</select>







</mapper>